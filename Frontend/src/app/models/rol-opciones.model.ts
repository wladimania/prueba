export interface RolOpciones {
  nombreOpcion: number; // Probablemente debería ser una cadena basada en el nombre
  idOpcion: number;
  // Incluir relaciones puede ser complejo y a menudo se omite por simplicidad
}
